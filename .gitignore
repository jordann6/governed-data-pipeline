# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
tfplan.json
crash.log
crash.*.log
*.tfvars
*.tfvars.json
# ...but the per-environment tfvars ARE the promotion structure and hold no
# secrets (env name, cost_center, DPU cap, retention), so track them.
!terraform/envs/*.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Cost gate artifacts (regenerated every `make gate`)
infracost.json
budget_env.json

# OS / editor
.DS_Store
*.swp
